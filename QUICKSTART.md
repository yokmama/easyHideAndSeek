# EasyHideAndSeek - クイックスタートガイド

## `/hs admin creategame` の使い方

### 必要な手順（順番通りに実行）

#### 1. 境界の設定
アリーナの範囲を決めます。2つの対角の角を指定します。

```
# 角1の地点に立って実行
/hs admin setpos1

# 角2の地点（対角）に立って実行
/hs admin setpos2
```

💡 **ヒント**: WorldEditのように、立方体の対角2点を指定します。

#### 2. スポーン地点の設定
プレイヤーが始まる場所を決めます。

```
# シーカー（鬼）のスポーン地点に立って実行
/hs admin setspawn seeker

# ハイダー（隠れる側）のスポーン地点に立って実行
/hs admin setspawn hider
```

💡 **ヒント**: 立っている位置と向き（カメラの方向）が保存されます。

#### 3. アリーナの作成

```
/hs admin creategame <アリーナ名>
```

**例**:
```
/hs admin creategame arena1
/hs admin creategame main_arena
```

### ✅ 作成が成功すると

以下のような表示が出ます：
```
Arena 'arena1' created!
- World: world
- Size: 50.0 blocks
- Seeker spawn: 100, 64, 100
- Hider spawn: 100, 64, 150
```

### ❌ エラーが出た場合

#### エラー: "Cannot create arena. Missing:"
**原因**: setpos1, setpos2, setspawn のいずれかが未設定

**解決方法**: エラーメッセージに表示される項目を設定してください
```
- pos1 is not set. Use /hs admin setpos1
- pos2 is not set. Use /hs admin setpos2
- seeker spawn is not set. Use /hs admin setspawn seeker
- hider spawn is not set. Use /hs admin setspawn hider
```

#### エラー: "All positions must be in the same world"
**原因**: 設定した位置が異なるワールドにある

**解決方法**: すべて同じワールド内で設定し直してください

#### エラー: "Arena 'xxx' already exists"
**原因**: 同じ名前のアリーナが既に存在する

**解決方法**: 
```
# 既存のアリーナを削除
/hs admin delete xxx

# または別の名前で作成
/hs admin creategame xxx2
```

### 📋 設定状況の確認

現在の設定状況を確認するコマンドはありませんが、以下で作成済みアリーナを確認できます：
```
/hs admin list
```

### 🎮 ゲームの開始

アリーナを作成したら、ゲームを開始できます：

```
# プレイヤーが参加（最低2人必要）
/hs join

# 管理者がゲーム開始
/hs admin start arena1
```

### 📖 完全な手順例

```bash
# 1. 境界設定
/hs admin setpos1          # X=100, Y=60, Z=100 の地点で
/hs admin setpos2          # X=150, Y=80, Z=150 の地点で

# 2. スポーン設定
/hs admin setspawn seeker  # X=125, Y=65, Z=125 の地点で
/hs admin setspawn hider   # X=125, Y=65, Z=140 の地点で

# 3. アリーナ作成
/hs admin creategame arena1

# 4. 確認
/hs admin list

# 5. ゲーム開始
/hs join
/hs admin start arena1
```

### 🔧 その他の管理コマンド

```
/hs admin list              # アリーナ一覧
/hs admin delete <name>     # アリーナ削除
/hs admin start <name>      # ゲーム強制開始
```

---

## よくある質問

**Q: setpos1とsetpos2はどのくらい離せばいい？**
A: 最低20ブロック程度。推奨は50-100ブロックです。

**Q: スポーン地点は境界内に置く必要がある？**
A: はい。境界外に置くとゲーム開始時に問題が発生する可能性があります。

**Q: 後からスポーン地点だけ変更できる？**
A: いいえ。アリーナを削除して作り直す必要があります。

**Q: 複数のアリーナを作成できる？**
A: はい。異なる名前で何個でも作成できます。
